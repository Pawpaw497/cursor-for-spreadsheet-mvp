# Cursor 行为准则与编码规范

## 方案设计原则
- **工业级标准**：在提供设计方案或重构建议时，必须优先对齐当前行业内的工业级最佳实践（Best Practices），考虑高可用性、可扩展性、安全性及性能优化。
- **架构对齐**：参考项目现有架构模式，避免引入非主流或过于实验性的实现方式。
- 遵循职责分离原则，不要在同一个文件中混合不同的功能。

## 代码规范
- **注释风格**：所有生成的代码注释必须严格遵守 **Google 开源项目风格指南 (Google Style Guides)**。
  - 包含清晰的函数说明、参数描述（@param）及返回值（@return）。
  - 注释应当简洁、准确，并解释“为什么”这么做，而不仅仅是“做了什么”。
- **文档一致性**：类和模块级别的文档块（Docstrings）需保持统一格式。

## 语言偏好
- **回复语言**：在 Chat 和检查代码时，请使用中文进行解释说明。

## 项目约定（与仓库文档一致）
- **文档同步**：每次功能修改后，在 `FEATURES.md` 与 `AGENT_IMPROVEMENTS.md` 中同步更新（不要求在聊天中展开细节）。
- **前端选型**：不造轮子，直接使用成熟的前端表格组件（如 React-Data-Grid、AG Grid 等）。
; - **范围与聚焦**：明确非目标（Non-goals），聚焦 MVP 与真实可跑 Demo；优先把一两件关键能力做完整。
- 后端仅使用 `server` 目录（原 server_py 已废弃，不再使用）。